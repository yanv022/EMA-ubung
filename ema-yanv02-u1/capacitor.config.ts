import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ema-yanv02-u1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
